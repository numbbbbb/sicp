(define (stream-limit s tolerance)
	(define (inner prev s)
		(if (< (abs (- (car-stream s) prev)) tolerance)
			(car-stream s)
			(inner (car-stream s) (cdr-stream s))
		)
	)
	(inner (car-stream s) (cdr-stream s))
)
