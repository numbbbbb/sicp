; it doesn't matter. because the list is generated firstly, and then tested through requirements. so whatever order will get the same result, because the order doesn't influence the generation process